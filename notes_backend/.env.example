# Optional configuration for notes backend

# Full SQLAlchemy database URL.
# Example for SQLite file:
# DATABASE_URL=sqlite:///notes.db
# Example for Postgres:
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/notes

# If DATABASE_URL is not set, a local SQLite file path is used.
# SQLITE_PATH=notes.db
